export default {
    plugins: {
        // Tailwind is now handled by Vite plugin
        autoprefixer: {}, // Autoprefixer is still good to have if not handled by lightningcss/vite built-in
    },
}
